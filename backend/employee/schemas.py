"""
Employee-specific Pydantic schemas (if needed beyond shared schemas).

For now, the Employee module re-uses schemas from backend/schemas.py.
Add employee-specific request/response models here as features grow.
"""
